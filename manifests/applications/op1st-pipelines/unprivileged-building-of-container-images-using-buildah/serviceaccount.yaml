apiVersion: v1
kind: ServiceAccount
metadata:
  name: pipelines-sa-userid-1000
  namespace: op1st-pipelines
# Registry credentials for the skopeo-copy push tasks. These were attached
# in-cluster out-of-band and absent from git until 2026-07-28; declared here so
# the SA is reproducible. The list is the UNION of what was already live plus
# `forgejo-pusher` — dropping an entry here detaches it on the next sync.
# OpenShift re-adds the auto-generated `…-dockercfg-*` token itself, so it is
# deliberately not listed.
secrets:
  - name: op1st-pipelines-coderberg-org
  - name: codeberg-pusher
  - name: forgejo-pusher
