apiVersion: v1
kind: ResourceQuota
metadata:
  name: small
spec:
  hard:
    requests.cpu: 2000m
    requests.memory: 1Gi
    limits.cpu: 2000m
    limits.memory: 2304Mi
    persistentvolumeclaims: "5"
    requests.storage: 50Gi
