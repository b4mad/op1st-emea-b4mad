apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

commonAnnotations:
  app.kubernetes.io/name: lvm-storage

resources:
  - operator-group.yaml
  - subscription.yaml
  - alerting-rules.yaml
